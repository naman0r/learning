package mcv;

/**
 * Interface for a MVC designed Todo list controller
 */
public interface IController {
  public void run(); // yeah this was a pretty useless interface to make
  // just need a run or go method, can maybe also add a reset method later on?
}
